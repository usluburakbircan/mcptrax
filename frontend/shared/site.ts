/**
 * Constants used by BOTH the app and the server (sitemap route, SEO tags).
 * They live in shared/ so server routes can import them without pulling
 * app-only auto-imports into the server tsconfig.
 */
export const SITE_URL = 'https://mcptrax.com'
export const SITE_NAME = 'mcptrax'
